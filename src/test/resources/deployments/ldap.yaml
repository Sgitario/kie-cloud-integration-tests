---
kind: Template
apiVersion: v1
metadata:
  name: ldap-template
  annotations:
    name: ldap-template
    description: Application template for ldap server
message: A new ldap server was created

parameters:
- name: APPLICATION_NAME
  displayName: Application Name
  description: The name for the application.
  required: true

objects:
- kind: ImageStream
  apiVersion: v1
  metadata:
    name: "rhba-qe-ldap"
    annotations:
      openshift.io/image.insecureRepository: 'true'
  spec:
    tags:
    - name: 'latest'
      from:
        kind: DockerImage
        name: bxms-binaries.usersys.redhat.com:5000/openldap:latest
      importPolicy:
        insecure: true
- kind: DeploymentConfig
  apiVersion: v1
  metadata:
    name: "${APPLICATION_NAME}-ldap"
    labels:
      application: "${APPLICATION_NAME}"
  spec:
    strategy:
      type: Recreate
    triggers:
    - type: ImageChange
      imageChangeParams:
        automatic: true
        containerNames:
        - "${APPLICATION_NAME}-ldap"
        from:
          kind: ImageStreamTag
          name: "rhba-qe-ldap:latest"
    - type: ConfigChange
    replicas: 1
    selector:
      deploymentConfig: "${APPLICATION_NAME}-ldap"
    template:
      metadata:
        name: "${APPLICATION_NAME}-ldap"
        labels:
          application: "${APPLICATION_NAME}"
          deploymentConfig: "${APPLICATION_NAME}-ldap"
      spec:
        containers:
        - name: "${APPLICATION_NAME}-ldap"
          image: "rhba-qe-ldap:latest"
          imagePullPolicy: Always
          ports:
          - name: "ldap"
            containerPort: 389
            protocol: TCP
          - name: "ldap-tls"
            containerPort: 636
            protocol: TCP
- kind: Service
  apiVersion: v1
  metadata:
    name: "${APPLICATION_NAME}-ldap-svc"
    labels:
      application: "${APPLICATION_NAME}"
  spec:
    ports:
    - name: ldap
      port: 389
      targetPort: 30389
    - name: ldap-tls
      port: 636
      targetPort: 30636
    selector:
      deploymentConfig: "${APPLICATION_NAME}-ldap"
- kind: Route
  apiVersion: v1
  metadata:
    name: "${APPLICATION_NAME}-ldap-route"
    labels:
      application: "${APPLICATION_NAME}"
      service: "${APPLICATION_NAME}-ldap-svc"
  spec:
      host: "${APPLICATION_NAME}-ldap.project.openshiftdomain"
      to:
        name: "${APPLICATION_NAME}-ldap-svc"
      port:
        targetPort: ldap